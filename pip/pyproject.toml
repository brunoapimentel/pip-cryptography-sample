[project]
name = "cryptography-example"
version = "0.1.0"
description = "A simple example project using the cryptography library"
requires-python = ">=3.11"
dependencies = [
    "cryptography",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
